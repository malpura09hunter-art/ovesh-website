const crypto=require('crypto');
const {S3Client,GetObjectCommand}=require('@aws-sdk/client-s3');
const {getSignedUrl}=require('@aws-sdk/s3-request-presigner');
function parseCookies(req){return Object.fromEntries(String(req.headers.cookie||'').split(';').map(x=>x.trim()).filter(Boolean).map(x=>{const i=x.indexOf('=');return i<0?[x,'']:[x.slice(0,i),decodeURIComponent(x.slice(i+1))]}))}
function session(req){const token=parseCookies(req).ovesh_cloud_session||String(req.headers.authorization||'').replace(/^Bearer\s+/i,'');const [payload,sig]=String(token).split('.');const secret=process.env.OVESH_CLOUD_SESSION_SECRET||process.env.OVESH_CLOUD_PASSWORD;if(!payload||!sig||!secret)throw Object.assign(new Error('Secure OVESH CLOUD session is unavailable.'),{statusCode:401});const expected=crypto.createHmac('sha256',secret).update(payload).digest('base64url');const a=Buffer.from(sig),b=Buffer.from(expected);if(a.length!==b.length||!crypto.timingSafeEqual(a,b))throw Object.assign(new Error('Secure OVESH CLOUD session is invalid.'),{statusCode:401});let d;try{d=JSON.parse(Buffer.from(payload,'base64url').toString('utf8'))}catch{throw Object.assign(new Error('Secure OVESH CLOUD session is invalid.'),{statusCode:401})}if(!d.exp||Date.now()>Number(d.exp))throw Object.assign(new Error('Secure OVESH CLOUD session has expired.'),{statusCode:401});return String(d.u||'OVESH').replace(/[^a-zA-Z0-9._-]/g,'_')}
function cfg(){const c={endpoint:String(process.env.B2_ENDPOINT||'').trim().replace(/\/$/,''),region:String(process.env.B2_REGION||'').trim(),bucket:String(process.env.B2_BUCKET||'').trim(),keyId:String(process.env.B2_KEY_ID||'').trim(),applicationKey:String(process.env.B2_APPLICATION_KEY||'').trim()};if(Object.values(c).some(v=>!v))throw Object.assign(new Error('Backblaze is not configured.'),{statusCode:500});return c}
function s3(){const c=cfg();return{c,s:new S3Client({region:c.region,endpoint:c.endpoint,forcePathStyle:false,credentials:{accessKeyId:c.keyId,secretAccessKey:c.applicationKey}})}}
function owned(uid,key){return key.startsWith(`users/${uid}/`)&&!key.includes('..')}
function mime(name){const e=String(name).split('.').pop().toLowerCase();return({jpg:'image/jpeg',jpeg:'image/jpeg',png:'image/png',gif:'image/gif',webp:'image/webp',svg:'image/svg+xml',pdf:'application/pdf',txt:'text/plain',md:'text/markdown',json:'application/json',csv:'text/csv',html:'text/html',css:'text/css',js:'text/javascript',mp4:'video/mp4',webm:'video/webm',mov:'video/quicktime',mp3:'audio/mpeg',wav:'audio/wav',m4a:'audio/mp4',pptx:'application/vnd.openxmlformats-officedocument.presentationml.presentation',ppt:'application/vnd.ms-powerpoint',doc:'application/msword',docx:'application/vnd.openxmlformats-officedocument.wordprocessingml.document',xls:'application/vnd.ms-excel',xlsx:'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'})[e]||'application/octet-stream'}
const safeName=n=>String(n||'file').split(/[\\/]/).pop().replace(/[^a-zA-Z0-9._ -]/g,'_').slice(0,180)||'file';
function signShare(uid,objectKey){const secret=process.env.OVESH_CLOUD_SESSION_SECRET||process.env.OVESH_CLOUD_PASSWORD;const payload=Buffer.from(JSON.stringify({uid,objectKey,iat:Date.now()})).toString('base64url');return `${payload}.${crypto.createHmac('sha256',secret).update(payload).digest('base64url')}`}
function verifyShare(token){const secret=process.env.OVESH_CLOUD_SESSION_SECRET||process.env.OVESH_CLOUD_PASSWORD;const [payload,sig]=String(token||'').split('.');if(!payload||!sig||!secret)throw Object.assign(new Error('Invalid share link.'),{statusCode:404});const expected=crypto.createHmac('sha256',secret).update(payload).digest('base64url');const a=Buffer.from(sig),b=Buffer.from(expected);if(a.length!==b.length||!crypto.timingSafeEqual(a,b))throw Object.assign(new Error('Invalid share link.'),{statusCode:404});let d;try{d=JSON.parse(Buffer.from(payload,'base64url').toString('utf8'))}catch{throw Object.assign(new Error('Invalid share link.'),{statusCode:404})}if(!d.objectKey||!d.uid||!owned(String(d.uid),String(d.objectKey)))throw Object.assign(new Error('Invalid share link.'),{statusCode:404});return d}
module.exports=async(req,res)=>{res.setHeader('Cache-Control','no-store');try{const action=String(req.query.action||req.body?.action||'');if(req.method==='POST'&&action==='create'){const uid=session(req),objectKey=String(req.body?.objectKey||'');if(!owned(uid,objectKey))return res.status(403).json({ok:false,error:'Access denied.'});const name=safeName(objectKey.split('/').pop().replace(/^[0-9a-f-]{36}-/i,''));const token=signShare(uid,objectKey);return res.status(200).json({ok:true,shareUrl:`${req.headers['x-forwarded-proto']||'https'}://${req.headers.host}/ovesh/share?token=${encodeURIComponent(token)}`,name})}
if(req.method==='GET'&&action==='open'){const d=verifyShare(req.query.token);const {c,s}=s3();const name=safeName(String(d.objectKey).split('/').pop().replace(/^[0-9a-f-]{36}-/i,''));const url=await getSignedUrl(s,new GetObjectCommand({Bucket:c.bucket,Key:d.objectKey,ResponseContentDisposition:`inline; filename="${name}"`,ResponseContentType:mime(name)}),{expiresIn:900});return res.status(200).json({ok:true,name,downloadUrl:url,expiresIn:900,storageProvider:'backblaze-b2'})}
return res.status(405).json({ok:false,error:'Method not allowed'});}catch(e){console.error('OVESH CLOUD SHARE FILE:',e);return res.status(Number(e.statusCode)||500).json({ok:false,error:e.message||'Unable to process share link.'})}};
